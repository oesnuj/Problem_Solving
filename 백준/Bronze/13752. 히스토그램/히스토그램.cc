#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    while (n--) {
        int k;
        cin >> k;
        while (k--) {
            cout << "=";
        }
        cout << "\n";
    }
    return 0;
}